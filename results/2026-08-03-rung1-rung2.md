# Rung 1 vs rung 2, 2026-08-03

## Setup

- **Device under test**: AMD Ryzen 7 9800X3D (8 cores / 16 threads), 62 GB RAM,
  Windows 11 Pro build 26200, .NET 10.0.302. NIC: Realtek PCIe 2.5GbE Family
  Controller, driver 1125.26.50.2025, linked at 1 Gbps.
- **Generator and sink**: NAS on the same switch, running `udpbench serve` in a
  container with host networking. Runs triggered over the HTTP control API.
- **Traffic**: 32-byte UDP payloads (78 bytes on the wire), one destination, so
  every received packet is forwarded exactly once.
- **Each data point**: 10 s measured, preceded by a discarded 3 s warmup against
  the same forwarder process, 4 sender threads.
- **Loss**: sender count vs sink count, both reported by the control API.
- **CPU**: forwarder process CPU seconds over the run, as a percentage of one
  core (the forwarders are single-threaded).

## Harness ceilings

Measured before the rungs, because a harness that cannot outrun the thing it
measures produces fiction:

| Component            | Ceiling      | Notes                                    |
| -------------------- | ------------ | ---------------------------------------- |
| Generator (16 thr)   | ~540,000 pps | ~42% of 1 GbE line rate at this size     |
| Sink                 | ~400,000 pps | single-threaded receive loop             |

Both sit well above the rungs measured here. They will not be enough for rungs
3 and up.

## Rung 1: naive UdpClient

| Offered pps | Sent pps | Delivered pps | Loss   | CPU (1 core) |
| ----------- | -------- | ------------- | ------ | ------------ |
| 100,000     | 99,985   | 99,728        | 0.27%  | 55.8%        |
| 150,000     | 149,976  | 147,626       | 1.58%  | 76.6%        |
| 175,000     | 174,974  | 166,684       | 4.75%  | 89.2%        |
| 200,000     | 199,939  | 163,347       | 18.31% | 90.0%        |

## Rung 2: frugal Socket + SocketAddress

| Offered pps | Sent pps | Delivered pps | Loss   | CPU (1 core) |
| ----------- | -------- | ------------- | ------ | ------------ |
| 100,000     | 99,985   | 99,574        | 0.42%  | 58.6%        |
| 150,000     | 149,975  | 148,191       | 1.20%  | 77.7%        |
| 175,000     | 174,984  | 158,514       | 9.42%  | 82.5%        |
| 200,000     | 199,966  | 166,869       | 16.56% | 89.5%        |

## Micro-benchmarks (BenchmarkDotNet, loopback send+receive per operation)

| Method          | Payload | Mean     | Ratio | Gen0   | Allocated | Alloc ratio |
| --------------- | ------- | -------- | ----- | ------ | --------- | ----------- |
| Rung1_UdpClient | 32 B    | 4.156 us | 1.00  | 0.0076 | 432 B     | 1.00        |
| Rung2_RawSocket | 32 B    | 4.081 us | 0.98  | -      | 72 B      | 0.17        |
| Rung1_UdpClient | 1200 B  | 4.307 us | 1.00  | 0.0305 | 1600 B    | 1.00        |
| Rung2_RawSocket | 1200 B  | 4.142 us | 0.96  | -      | 72 B      | 0.04        |

## Reading

Rung 2 eliminates the per-datagram garbage (432 B to 72 B at 32 bytes, 1600 B to
72 B at 1200 bytes, and no Gen0 collections at all) and this buys 2-4% of
per-operation time and nothing measurable in throughput or CPU. Both rungs carry
roughly 150,000 pps cleanly, both start losing packets around 175,000, and both
peg a single core at about 90% doing it.

The syscall pair dominates: roughly 4 us per datagram either way. Allocation was
never the bottleneck at this rate, so removing it could not move the number.

## Caveats

- The device under test was not otherwise idle (no fixed power plan, no core
  pinning). Repeat runs vary by a percent or two, visible in the non-monotonic
  loss figures around the cliff.
- Micro-benchmark timings cover a loopback send plus receive, not wire
  performance; only the allocation columns transfer to the LAN setting.

## Follow-up: where did the last 72 bytes go?

Splitting the micro-benchmark shows `SendToAsync` allocates nothing (it completes
synchronously) and `ReceiveFromAsync` accounts for the whole 72 B, because it
suspends and the state machine is boxed. A fully synchronous variant allocates
zero and is marginally faster (4.042 us vs 4.090 us).

| Method                 | Payload | Mean     | Allocated |
| ---------------------- | ------- | -------- | --------- |
| Rung1_UdpClient        | 32 B    | 4.154 us | 432 B     |
| Rung2_RawSocket        | 32 B    | 4.090 us | 72 B      |
| Rung2_SendOnly         | 32 B    | 4.115 us | -         |
| Rung2_ReceiveOnly      | 32 B    | 4.072 us | 72 B      |
| Rung2_FullySynchronous | 32 B    | 4.042 us | -         |

That 72 B is per async method invocation, not per datagram: this benchmark shape
invokes one async method per operation, while the forwarder runs its whole loop
inside a single long-lived one. BenchmarkDotNet models that with
`OperationsPerInvoke`, looping inside the benchmark and dividing the results:

| Method                     | Payload | Mean     | Allocated |
| -------------------------- | ------- | -------- | --------- |
| Rung1_UdpClient_Loop       | 32 B    | 4.079 us | 360 B     |
| Rung2_RawSocket_Loop       | 32 B    | 4.048 us | -         |

Rung 2 allocates nothing per datagram. Rung 1 drops from 432 B to 360 B, the
difference being exactly the amortized state machine box (432 - 72 = 360), which
leaves the genuine per-datagram garbage: the receive buffer and the IPEndPoint.

The running forwarders agree, measured in-process at a sustained 150,000 pps:

| Rung | Allocation rate | Gen0 collections |
| ---- | --------------- | ---------------- |
| 1    | 36.8 MB/s       | 14 per second    |
| 2    | 0.0 MB/s        | 0                |

Rung 2 is genuinely allocation-free in steady state. There is nothing left to remove.

## Harness capacity for rungs 3 and up

1 GbE ceilings, for reference:

| Payload | On the wire | Line rate  |
| ------- | ----------- | ---------- |
| 32 B    | 98 B        | 1,275,510 pps |
| 512 B   | 578 B       | 216,263 pps   |
| 1472 B  | 1538 B      | 81,274 pps    |

Against that: the generator does ~540,000 pps and the sink ~400,000 pps. Fine for
rungs 1 and 2 (cliff at ~175,000 pps, roughly 3x headroom) and not enough to find
the maximum of a batched forwarder.
